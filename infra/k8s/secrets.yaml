apiVersion: v1
kind: Secret
metadata:
  name: taskmanager-secrets
  namespace: taskmanager
type: Opaque
stringData:
  # Database credentials
  postgres-user: taskapp
  postgres-password: taskpass  # CHANGE THIS IN PRODUCTION!
  postgres-db: taskdb
  
  # Application secrets
  session-secret: change_me_in_production_please  # CHANGE THIS IN PRODUCTION!
  jwt-secret: change_me_in_production_please  # CHANGE THIS IN PRODUCTION!

